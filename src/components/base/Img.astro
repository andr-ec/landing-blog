---
// import { Image } from "@astrojs/image/components";

const props = Astro.props;
// const classMap = {
// };

// const newClass = Object.hasOwn(classMap, props.class)
// 	? classMap[props.class]
// 	: props.class;
---

<img src={props.src} class="object-contain object-top" alt={props.alt} />
